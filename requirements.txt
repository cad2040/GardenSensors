# Core Data Processing and Visualization
pandas>=1.3.0
numpy>=1.20.0
matplotlib>=3.4.0
bokeh>=3.4.0

# Database and Network
pymysql>=1.0.2
mysql-connector-python>=8.0.0
requests>=2.26.0
tenacity>=8.2.0
python-dotenv>=1.0.0

# Testing and Development
pytest>=7.4.3
black>=23.11.0
setuptools>=78.1.0

# Optional Hardware Support
# RPi.GPIO>=0.7.0  # Uncomment when deploying to Raspberry Pi

# Note: Some packages like 'ftplib' are part of Python's standard library
# and don't need to be listed here.

# Dependencies will be installed in a virtual environment
# To install: python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt 